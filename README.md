**IPERF WIFI SLED TEST**
From .exe directory, call iperf_test from CL.
Arguments:
  -c: Client IP Address (Required)
  -s: Server IP Address (Required)

  example: C:\(executable_path)> iperf_test -c 172.16.4.192 -s 172.16.4.137

  Results are printed on screen
